# RookieAuctions 沉浸式拍卖配置与测试指南

本文用于在独立测试服上配置和验收 RookieAuctions 的定时沉浸式拍卖功能。生产服升级前，请先备份插件配置和数据库。

## 1. 测试环境

- 服务端：Paper 或 Purpur 1.21.4
- Java：21
- 必需插件：Vault，以及一个支持 Vault 的经济插件
- 可选插件：PlaceholderAPI
- 建议至少准备 3 个测试账号：卖家、买家 A、买家 B

将 `RookieAuctions.jar` 放入 `plugins` 目录后完整启动一次服务器，使插件生成默认配置。不要使用 Bukkit `/reload`；修改配置后应正常关闭并重新启动服务器。

### 测试前备份

至少备份以下内容：

```text
plugins/RookieAuctions/config.yml
plugins/RookieAuctions/sqlite.db
```

如果使用 MariaDB，请备份对应数据库。建议使用独立测试数据库，不要直接操作生产库。

## 2. 默认场次规则

默认配置如下：

| 配置 | 默认值 | 含义 |
| --- | ---: | --- |
| `immersive.timezone` | `Asia/Shanghai` | 场次所使用的自然日和时区 |
| `immersive.schedules.afternoon.time` | `14:00` | 午场开始时间 |
| `immersive.schedules.evening.time` | `20:00` | 晚场开始时间 |
| `immersive.booking-horizon-sessions` | `2` | GUI 中开放的未来可投稿场次数 |
| `immersive.capacity-per-session` | `16` | 每场最大拍品数 |
| `immersive.max-lots-per-player-per-session` | `2` | 单个卖家每场投稿上限 |
| `immersive.submission-cutoff-seconds` | `600` | 开场前 10 分钟锁单 |
| `immersive.lot-duration-seconds` | `120` | 每件拍品竞拍时间 |
| `immersive.intermission-seconds` | `10` | 两件拍品之间的换品时间 |
| `immersive.missed-start-grace-seconds` | `1800` | 错过开场后的补开宽限 |
| `immersive.blocked-retry-seconds` | `30` | 场地无效时的重试间隔 |
| `antisnipe.seconds-for-start` | `30` | 防秒拍触发窗口 |
| `antisnipe.time` | `30` | 触发后重置到的剩余秒数 |
| `antisnipe.run-times` | `3` | 每件拍品最多触发次数 |

满场 16 件的基础时长为 `16 × 120 + 15 × 10 = 2070` 秒，即 34 分 30 秒。一口价会缩短场次，防秒拍会延长场次。

## 3. 配置拍卖场地

沉浸模式默认关闭，而且没有危险的默认坐标。使用管理员账号站在目标位置执行以下命令：

```text
/auction admin venue set buyer-spawn
/auction admin venue set item-display
/auction admin venue set info-display
/auction admin venue set corner1
/auction admin venue set corner2
```

各点用途：

| 场地点 | 用途 |
| --- | --- |
| `buyer-spawn` | 买家开场传送点，必须位于 corner1/corner2 定义的区域内 |
| `item-display` | 当前拍品的 ItemDisplay 位置及朝向 |
| `info-display` | 拍卖信息 TextDisplay 位置及朝向 |
| `corner1` | 会场区域第一个角点 |
| `corner2` | 会场区域第二个角点 |

五个点必须位于同一个已加载世界。区域边界包含两个端点，不要求先设置较小坐标；插件会自动规范最小值和最大值。

设置完成后执行：

```text
/auction admin venue validate
/auction admin venue preview
/auction admin venue enable
```

预期结果：

- `validate` 提示场地有效。
- `preview` 生成一个测试拍品展示和信息板。
- `enable` 成功后，`immersive.enabled` 变为 `true`。
- 当前拍品展示缓慢旋转，默认 80 tick 转一圈。

展示样式可以直接在 `config.yml` 中调整：

```yaml
immersive:
  venue:
    item-display:
      scale: 1.25
      spin-period-ticks: 80
    info-display:
      line-width: 240
      shadowed: true
      see-through: false
      background-color: '#66000000'
```

## 4. 快速测试配置

不要为了测试等待到 14:00 或 20:00。请在独立测试服、首次生成场次前，把两场时间改为当前时间之后的两个整分钟时间，例如第一场在 3 分钟后、第二场在 8 分钟后。

下面是一份适合快速验收的示例。将 `HH:mm` 替换为实际未来时间：

```yaml
immersive:
  enabled: false
  timezone: 'Asia/Shanghai'
  booking-horizon-sessions: 2
  capacity-per-session: 3
  max-lots-per-player-per-session: 2
  submission-cutoff-seconds: 60
  lot-duration-seconds: 30
  intermission-seconds: 5
  missed-start-grace-seconds: 120
  blocked-retry-seconds: 10
  schedules:
    afternoon:
      display-name: '测试一场'
      time: 'HH:mm'
    evening:
      display-name: '测试二场'
      time: 'HH:mm'

antisnipe:
  config-version: 2
  enabled: true
  seconds-for-start: 10
  time: 10
  run-times: 3
```

注意：

- 场次时间只支持 `HH:mm`。
- 防秒拍触发时间和目标时间不能大于单件竞拍时间。
- 卖家上限不能大于场次容量。
- 锁单时间、场次数、容量、卖家上限和单件时长必须大于 0。
- 已经持久化的 `yyyy-MM-dd/afternoon` 或 `yyyy-MM-dd/evening` 场次不会因修改配置而移动。
- 如果测试服已经生成了当天场次，最安全的方法是关闭服务器，将测试服的 SQLite 数据库重命名为备份文件后重新启动；不要删除生产数据库。MariaDB 应改用新的测试数据库。

修改并重启后，重新设置/验证场地，再执行：

```text
/auction admin session status
```

确认显示的场次时间、状态和容量符合测试配置。

## 5. 基础功能验收

### 5.1 投稿与容量

1. 卖家执行 `/auction` 打开 GUI。
2. 进入“拍卖场次”，选择未来场次后点击投稿；沉浸模式会打开“一页投稿”界面。
3. 在同一页设置公开或密封模式、起拍价、最低加价和一口价；数字字段只在需要修改时打开输入框。
4. 检查物品与上架费是否只扣除一次。
5. 在锁单前撤稿，确认物品进入领奖箱、名额释放、上架费不退。
6. 同一卖家提交第 2 件，应成功；提交第 3 件，应被拒绝。
7. 场次达到容量后再提交一件，应被拒绝，且不能扣物品或费用。

沉浸模式的一页投稿界面会默认选择最近仍有空位的场次；点击场次卡可以切换目标。投稿成功后物品选择和价格会重置，点击“沿用上次设置”只复制模式、价格和一口价，不会覆盖新选择的物品。

锁单后确认：

- 不能继续投稿或撤稿。
- 已成功投稿的顺序保持 FIFO。
- 0 件拍品的场次会跳过，不会传送报名玩家。

### 5.2 买家报名与传送

1. 买家 A 在 `/auction` GUI 中报名即将开始的场次。
2. 买家 B 不报名。
3. 开场时保持两人在线。

预期结果：

- 买家 A 只被传送一次，并进入拍卖模式。
- 买家 B 不会被自动传送。
- 买家 B 可以在场次进行中通过 GUI 临时加入并传送。
- 投稿卖家不会因为投稿而自动报名。

### 5.3 F 键竞价面板

进入拍卖模式后按交换副手键（默认 F）：

- 原交换副手动作被取消。
- 下一 tick 打开 27 格加价面板。
- 面板显示拍品、余额、单件倒计时、整场预计剩余、最低有效价、`+1/+5/+10`、自定义价、一口价、返回会场和退出按钮。
- 打开箱子、铁砧等其他容器时按 F，不应劫持容器操作。
- 非拍卖模式玩家按 F，保持原版行为。

走出 corner1/corner2 区域后：

- 玩家仍处于拍卖模式。
- 出价按钮变为不可用，核心出价返回 `NOT_IN_VENUE`。
- 点击“返回会场”可传送回 `buyer-spawn`。

执行 `/auction leave` 后，玩家应退出拍卖模式并返回入场前的位置。

### 5.4 公开与密封拍卖

公开拍卖检查：

- 信息板和 GUI 显示公开当前价。
- 有效出价后价格、倒计时和修订状态同步刷新。

密封拍卖检查：

- 公共信息板只显示“当前报价：已密封”。
- PAPI 公共报价不泄露最高金额或最高出价者。
- 买家个人面板只显示自己的最高报价、出价次数和剩余机会。
- 其他买家的金额不能出现在公共 GUI、信息板或广播中。

### 5.5 防秒拍与一口价

使用快速配置时，在剩余时间低于 10 秒后提交有效出价：

- 剩余时间重置到 10 秒。
- 每件拍品最多重置 3 次。
- 第 4 次末段出价仍可有效，但不再重置时间。

点击一口价后：

- 当前拍品立即结束。
- 奖励只生成一次。
- 如果还有下一件，进入配置的换品倒计时。
- 最后一件结束后，整场结束并安排买家返程。

## 6. 信息板与 PlaceholderAPI

信息板应每秒显示并更新：

- 当前场次
- 第几件/总件数
- 当前拍品
- 单件倒计时
- 当前报价或“已密封”
- 公开/密封模式
- 整场动态预计剩余时间

安装 PlaceholderAPI 后，可以分别测试两个命名空间：

```text
%rookieauctions_session_state%
%rookieauctions_session_start_time%
%rookieauctions_session_estimated_remaining%
%rookieauctions_session_lot_progress%
%rookieauctions_session_capacity%
%rookieauctions_next_session_time%
%rookieauctions_session_current_mode%
%rookieauctions_session_current_bid%

%ezauctions_session_state%
%ezauctions_session_lot_progress%
%ezauctions_session_current_bid%
%ezauctions_remainingtime%
```

其中旧 `%ezauctions_remainingtime%` 继续表示当前单件拍品剩余秒数。

## 7. 重启与故障恢复验收

以下测试只应在独立测试服进行。每次测试都使用服务器的正常停止/启动流程；需要模拟崩溃时，先复制数据库备份。

### 7.1 当前拍品恢复

1. 开始一场至少有 2 件拍品的场次。
2. 买家完成一次有效出价。
3. 在当前拍品尚未结束时重启服务器。

检查：

- 仍恢复到同一场、同一件拍品。
- 已落库报价按顺序恢复，不重复扣款。
- 倒计时、防秒拍次数和修订号继续。
- 后续拍品仍按原顺序进行。

### 7.2 换品阶段恢复

在 5 秒换品倒计时期间重启，确认重启后继续换品阶段，不重复结算上一件，也不跳过下一件。

### 7.3 买家返程恢复

1. 买家进入拍卖模式后下线。
2. 让整场结束。
3. 买家重新上线。

确认玩家被送回持久化的入场前位置，并且不再拥有竞价权限。

### 7.4 场地失效

1. 使用 `/auction admin venue disable` 暂时关闭沉浸场地，或在测试服制造一个无效世界配置。
2. 等待场次到达开场时间。

确认：

- 场次不会错误开场，也不会传送买家。
- 管理员能看到场地无效提示。
- 修复坐标并执行 `/auction admin venue validate`、`/auction admin venue enable` 后，调度器立即重新检查。

## 8. 数据库检查

测试时重点观察以下独立表是否出现并持续更新：

```text
ezAuctions_Session
ezAuctions_SessionLot
ezAuctions_Attendance
ezAuctions_RuntimeCheckpoint
ezAuctions_BidTransaction
ezAuctions_SubmissionTransaction
```

不要手工修改运行中的数据库。需要测试 SQLite 与 MariaDB 时，应分别使用独立环境重复投稿、出价、重启、退款和结算流程。

验收重点：

- 同一次投稿、出价和结算只有一个最终结果。
- 邮箱奖励 ID 保持确定性，不出现重复物品或重复金币。
- 未完成的 Vault 扣款在启动时产生管理员告警并执行保守补偿。
- 已完成场次不会因重复启动而再次开场。

## 9. 常见问题

### 场次时间修改后没有变化

已生成且已经持久化的场次时间不会移动。测试服可以换用全新的测试数据库；生产服应等待新生成的后续场次。

### 场次使用了 14:00/20:00 默认值

通常表示配置无效并触发了安全默认值。检查服务器日志，并确认：

- 时区名称有效。
- 时间采用 `HH:mm`。
- 两个场次不能配置为相同时间。
- 防秒拍时间不大于单件竞拍时间。
- 所有要求为正数的配置均大于 0。

### 无法启用场地

执行 `/auction admin venue validate` 查看具体缺失项。重点检查五个点是否齐全、世界是否已加载，以及 `buyer-spawn` 是否位于区域内。

### F 键没有打开面板

确认玩家已成功进入当前运行场次的拍卖模式，并且没有打开箱子、铁砧等其他容器。按键被重新绑定也没有影响，只要触发的是“交换副手”操作即可。

### 玩家无法出价

依次检查：

1. 场次是否为 `RUNNING`。
2. 玩家是否成功进入拍卖模式。
3. 玩家是否位于 corner1/corner2 区域内。
4. GUI 中的拍品 ID 和修订号是否仍为最新。
5. Vault 余额是否足够。

## 10. 生产配置恢复清单

快速测试结束后，将配置恢复为生产值：

```yaml
immersive:
  timezone: 'Asia/Shanghai'
  booking-horizon-sessions: 2
  capacity-per-session: 16
  max-lots-per-player-per-session: 2
  submission-cutoff-seconds: 600
  lot-duration-seconds: 120
  intermission-seconds: 10
  missed-start-grace-seconds: 1800
  blocked-retry-seconds: 30
  schedules:
    afternoon:
      display-name: '午场'
      time: '14:00'
    evening:
      display-name: '晚场'
      time: '20:00'

antisnipe:
  config-version: 2
  enabled: true
  seconds-for-start: 30
  time: 30
  run-times: 3
```

最终上线前确认：

- [ ] 已备份生产配置和数据库
- [ ] 使用 Java 21 与 Paper/Purpur 1.21.4
- [ ] Vault 与经济插件正常
- [ ] 五个场地点已验证
- [ ] 密封报价不会公开泄露
- [ ] 报名玩家只传送一次
- [ ] 场外玩家无法出价
- [ ] `/auction leave` 和离线返程正常
- [ ] 当前拍品重启恢复正常
- [ ] 16 件完整场次顺序正常

## 11. Mineflayer 冒烟与完整矩阵测试

仓库提供了可丢弃的 Paper 1.21.4 测试服和 Mineflayer bot，详见
[`integration/README.md`](integration/README.md)。在仓库根目录执行：

```powershell
mvn -q -DskipTests package
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\integration\run-smoke.ps1 -Reset
```

冒烟脚本只验证插件加载、Vault/EssentialsX 经济挂接、场地五点设置与校验、场次状态和
`/auction` GUI。要测试真实投稿、竞价和结算矩阵，执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\integration\run-smoke.ps1 -FullMatrix
```

完整矩阵会用 2 个卖家和 2 个买家验证每卖家 2 件限制、满场拒绝、撤稿补位、公开/密封出价、
一口价、强制传送和领奖箱结算，并明确检查“第一场已满时向第二场投稿仍成功”，用于排查把多场
拍品错误相加的容量问题。测试服只监听本机，生成的世界和数据库位于 `integration/server`，
不会提交到仓库。失败时以 Mineflayer 日志、Paper 日志和 SQLite 状态为准，不把客户端时序超时
冒充插件缺陷。
